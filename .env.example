# Firebase configuration is not used for this marketing site.
# Hosting is AWS S3 + CloudFront. CMS content seeds from content/*.json
# into local browser storage for admin edits during local development.
#
# Copy to .env.local only if you intentionally enable an alternate backend.
